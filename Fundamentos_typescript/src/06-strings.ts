(()=> {
  let productTitle = 'My amazing product'
  console.log(productTitle);

  const productDescrip = "Bla bla bla";

  const summary= `
    Nunca
    pares
    de
    aprender
    Titulo:${productTitle}
  `;

  console.log(summary)

}) ();
