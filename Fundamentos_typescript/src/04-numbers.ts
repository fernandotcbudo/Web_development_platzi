(() => {
  let productPrice= 100;
  productPrice = 110;
  productPrice = productPrice + 1;
  console.log('Price:',productPrice);

  let productStore: number;

  let discount = parseInt('123');
  if (discount > 100){
    console.log('It is bigger');
  }
}) ();
